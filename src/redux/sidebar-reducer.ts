import {DialogActionTypes} from "./dialogs-reducer";

export type SidebarType = {}

let initialState = {}

export const sidebarReducer = (state:SidebarType = initialState, action:DialogActionTypes ) => {
    return state
}